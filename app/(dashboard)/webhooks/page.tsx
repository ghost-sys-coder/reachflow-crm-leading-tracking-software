import{Webhook}from"lucide-react";import{getAuthedOrgClient}from"@/lib/auth/org";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{CreateEndpointForm,RetryDeliveryForm,RotateSecretForm,SubscriptionForm,TestEndpointForm,ToggleEndpointForm}from"@/components/webhooks/webhook-action-forms"
export default async function WebhooksPage(){const{ctx}=await getAuthedOrgClient();if(!ctx)return null;const[{data:endpoints},{data:deliveries}]=await Promise.all([ctx.supabase.from("webhook_endpoints").select("id,name,url,is_active,failure_count,last_success_at,last_failure_at,created_at,subscribed_events,secret_last_four").eq("org_id",ctx.orgId).order("created_at",{ascending:false}),ctx.supabase.from("webhook_deliveries").select("id,status,attempt_count,last_error,created_at,webhook_endpoints(name),webhook_events(event_type),webhook_delivery_attempts(response_code,duration_ms,error,attempted_at)").eq("org_id",ctx.orgId).order("created_at",{ascending:false}).limit(50)]);return <div className="space-y-6"><div><h1 className="flex items-center gap-2 text-2xl font-semibold"><Webhook/>Webhook delivery center</h1><p className="text-muted-foreground">Signed HTTPS delivery, health, bounded response diagnostics, and guarded retries.</p></div>{!process.env.WEBHOOK_ENCRYPTION_KEY&&<div className="rounded-lg border border-amber-500/30 bg-amber-500/10 p-3 text-sm text-amber-800">Set WEBHOOK_ENCRYPTION_KEY before creating endpoints. Use a long random production secret and never expose it to the browser.</div>}{ctx.role==="admin"&&<Card><CardHeader><CardTitle>Add endpoint</CardTitle></CardHeader><CardContent><CreateEndpointForm/><p className="mt-2 text-xs text-muted-foreground">HTTPS only. Private networks, credentials in URLs, redirects, and non-standard ports are rejected.</p></CardContent></Card>}<div className="grid gap-3 lg:grid-cols-2">{endpoints?.map(e=><Card key={e.id}><CardHeader><CardTitle className="flex justify-between"><span>{e.name}</span><span className={e.is_active?"text-emerald-600":"text-muted-foreground"}>{e.is_active?"Active":"Inactive"}</span></CardTitle></CardHeader><CardContent className="space-y-3"><p className="truncate text-sm text-muted-foreground">{e.url}</p><p className="text-xs">Failures: {e.failure_count} · Last success: {e.last_success_at?new Date(e.last_success_at).toLocaleString():"Never"}</p>{ctx.role==="admin"&&<div className="flex gap-2"><TestEndpointForm id={e.id}/><ToggleEndpointForm id={e.id} active={e.is_active}/><RotateSecretForm id={e.id}/></div>}<SubscriptionForm id={e.id} selected={e.subscribed_events??[]}/></CardContent></Card>)}</div><Card><CardHeader><CardTitle>Delivery ledger</CardTitle></CardHeader><CardContent className="space-y-2">{deliveries?.map(d=><div key={d.id} className="grid gap-2 rounded-lg border p-3 md:grid-cols-[1fr_auto_auto]"><div><b>{d.webhook_events?.[0]?.event_type}</b><p className="text-xs text-muted-foreground">{d.webhook_endpoints?.[0]?.name} · {new Date(d.created_at).toLocaleString()}</p>{d.last_error&&<p className="mt-1 text-xs text-destructive">{d.last_error}</p>}</div><span className="text-sm font-medium">{d.status} · {d.attempt_count} attempts</span>{ctx.role==="admin"&&d.status!=="delivered"&&<RetryDeliveryForm id={d.id}/>}</div>)}{!deliveries?.length&&<p className="py-8 text-center text-muted-foreground">No deliveries yet.</p>}</CardContent></Card></div>}




